name: Convert Surge to Egern

on:
  push:
    paths:
      - 'Surge/**.list'
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Convert files
        run: |
          python Scripts/Surge2Egern.py \
            --pattern "Surge/*.list" \
            --skip-no-resolve-files '["Surge/China.list"]'

      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add Egern/*.yaml
          git commit -m "Convert Surge rules to Egern format" || echo "No changes to commit"
          git push origin main
